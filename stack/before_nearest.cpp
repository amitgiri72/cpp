#include<bits/stdc++.h>
using namespace std;
int before_smallest(int arr[],int n)
{
    int mini=INT_MAX;
    for(int i=0;i<n;i++)
    {
        if(arr[i]<mini)
    }
}
int main()
{
    int arr[]={5,43,23,5,99,4,55};
    int n=sizeof(arr)/sizeof(arr[0]);
    return before_smallest(arr,n);
    return 0;
}